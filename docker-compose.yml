services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: chantrale
      POSTGRES_PASSWORD: chantrale
      POSTGRES_DB: chantrale
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
